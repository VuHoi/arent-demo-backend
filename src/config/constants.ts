export const DEFAULT_USER_PICTURE = 'https://www.gravatar.com/avatar/default?s=200&d=mp';

export const CONTROLLERS = {
  MENU: 'menu',
};

export enum STATUS {
  ENABLED = 'enabled',
  DISABLED = 'disabled',
}
